{% extends 'SMAdminBundle::admin.html.twig' %}
{% block body %}
<h1>Product creation</h1>

<form action="{{ path('admin_products_new') }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
    <fieldset>
        <div class="control-group">
            <label class="control-label" for="input01">{{ form_label(form.branch) }}</label>
            <div class="controls">
                {{ form_errors(form.branch) }}
                {{ form_widget(form.branch) }}
            </div>
        </div>

        <div class="control-group">
            <label class="control-label" for="input01">{{ form_label(form.productgroup) }}</label>
            <div class="controls">
                {{ form_errors(form.productgroup) }}
                {{ form_widget(form.productgroup) }}
            </div>
        </div>

        <div class="control-group">
            <label class="control-label" for="input01">{{ form_label(form.price) }}</label>
            <div class="controls">
                {{ form_errors(form.price) }}
                {{ form_widget(form.price) }}
            </div>
        </div>

        <div class="control-group">
            <label class="control-label" for="input01">{{ form_label(form.unit) }}</label>
            <div class="controls">
                {{ form_errors(form.unit) }}
                {{ form_widget(form.unit) }}
            </div>
        </div>

        <div class="control-group">
            <label class="control-label" for="input01">{{ form_label(form.discount) }}</label>
            <div class="controls">
                {{ form_errors(form.discount) }}
                {{ form_widget(form.discount) }}
            </div>
        </div>

        <div class="control-group">
            <label class="control-label" for="input01">{{ form_label(form.amount) }}</label>
            <div class="controls">
                {{ form_errors(form.amount) }}
                {{ form_widget(form.amount) }}
            </div>
        </div>

        <div class="control-group">
            <label class="control-label" for="input01">{{ form_label(form.thumb) }}</label>
            <div class="controls">
                {{ form_errors(form.thumb) }}
                {{ form_widget(form.thumb) }}
            </div>
        </div>

        <div class="control-group">
            <label class="control-label" for="input01">{{ form_label(form.status) }}</label>
            <div class="controls">
                {{ form_errors(form.status) }}
                {{ form_widget(form.status) }}
            </div>
        </div>

        <div class="control-group">
            <label class="control-label" for="optionsCheckbox">Images</label>
            <div class="controls">
              <label class="checkbox">
                    {{ form_errors(form.media_id) }}
                    {{ form_widget(form.media_id, {'attr': {'style': 'display: none'} }) }}
                    {% set selectName = form.media_id.get('full_name') %}
                    {{ smmedia(optMedias, optMediaTypes, selectName, mediaPath, {'multiple': true })|raw() }}
              </label>
            </div>
        </div>

        <hr />

        <div class="tabbable"> <!-- Only required for left/right tabs -->
            <ul class="nav nav-tabs">
                  {% set isActive = 1 %}
                  {% for lang in langList %}
                      {% if isActive == 1%}
                          <li class="active"><a href="#tab{{ lang.id }}" data-toggle="tab">{{ lang.name }}</a></li>
                      {% else %}
                          <li><a href="#tab{{ lang.id }}" data-toggle="tab">{{ lang.name }}</a></li>
                      {% endif %}
                      {% set isActive = 2 %}
                  {% endfor %}
            </ul>
            <div class="tab-content">
                {% set isActive = 1 %}
                {% for lang in langList %}
                    {% set currentAL = null %}
                    {% for productsLanguage in form.product_languages%}
                        {% if productsLanguage.vars.value.language.id == lang.id %}
                            {% set currentAL = productsLanguage %}
                        {% endif%}
                    {% endfor %}
                    {% if isActive == 1%}
                        <div class="tab-pane active" id="tab{{ lang.id }}">
                    {% else %}
                        <div class="tab-pane" id="tab{{ lang.id }}">
                    {% endif %}
                            <fieldset>
                                {% if currentAL is not null %}
                                <div class="control-group">
                                    <label class="control-label" for="input01">{{ form_label(currentAL.name) }}</label>
                                    <div class="controls">
                                        {{ form_errors(currentAL.name) }}
                                        {% if defaultLanguage.name == currentAL.vars.value.language.name %}
                                            {{ form_widget(currentAL.name, {'attr': {'required': 'required'}}) }}
                                        {% else %}
                                            {{ form_widget(currentAL.name) }}
                                        {% endif %}
                                    </div>
                                </div>

                                <div class="control-group">
                                    <label class="control-label" for="input01">{{ form_label(currentAL.description) }}</label>
                                    <div class="controls">
                                        {{ form_errors(currentAL.description) }}
                                        {{ form_widget(currentAL.description) }}
                                    </div>
                                </div>

                                {% endif%}
                            </fieldset>
                        </div>
                    {% set isActive = 2 %}
               {% endfor %}
            </div>
        </div>

        <div class="form-actions">
            {{ form_row(form._token) }}
            <button type="submit" class="btn btn-primary">Save changes</button>
            <a href="{{ path('admin_products', {'page' : 1}) }}" class="btn">Back To List</a>
        </div>

    </fieldset>
</form>

{% endblock %}